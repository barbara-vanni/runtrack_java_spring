spring:
  application:
    name: "jour2"
  profiles:
    active: dev
  devtools:
    restart:
      enabled: true
  thymeleaf:
      enabled: true
      cache: false

---
spring:
  config:
    activate:
        on-profile: dev

---
spring:
    config:
        activate:
            on-profile: prod
